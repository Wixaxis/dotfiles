# /home/wixaxis/.config/tmuxinator/rails-starter.yml

name: rails-starter
root: ~/Projects/rails-starter

windows:
  - editor: nvim .
  - opencode: opencode -c
  - terminal:
  - server:
      layout: main-horizontal
      panes:
        - bin/dev || rails server
        -
