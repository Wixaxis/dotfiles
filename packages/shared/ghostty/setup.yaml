---
name: ghostty
description: Ghostty terminal configuration
state: active
engine: native
applies_when: {}
install:
  arch:
    manager: paru
    packages:
    - ghostty
  macos:
    manager: brew
    casks:
    - ghostty
links:
- source: stow/.config
  target: ".config"
  mode: children
  create_parents: true
  backup_on_conflict: true
