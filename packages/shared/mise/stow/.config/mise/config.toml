[settings]
idiomatic_version_file_enable_tools = ["ruby", "node"]
experimental = true

[settings.ruby]
# Ruby compilation is enabled by default, no need to specify
# Use ruby_install_opts for custom compilation options if needed

[tools]
pnpm = "latest"
python = "latest"
ruby = "3.4.8"
