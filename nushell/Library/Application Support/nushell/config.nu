# config.nu
#
# macOS-specific wrapper for nushell configuration
# This file sources the main config from ~/.config/nushell/config.nu
# which works on both macOS and Linux

# Source the main config file from ~/.config/nushell/config.nu
source ~/.config/nushell/config.nu
