# /home/wixaxis/.config/tmuxinator/notesman.yml

name: notesman
root: ~/projects/notesman

windows:
  - editor: nvim .
  - opencode: opencode -c
  - terminal:
  - server:
      layout: main-horizontal
      panes:
        - bin/dev || rails server
        -
